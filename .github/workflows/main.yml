name: CI
on:
  workflow_dispatch:
        
jobs:
  build:
    runs-on: [self-hosted, normal]
    environment: test
    steps:
      - run: echo "${{ toJSON(github) }}"
      - run: echo "${{ toJSON(job) }}"
      - run: echo "finish"
